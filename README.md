# ExtractingPublicS3Objects
This repository consist of single script which is used to iterate through a list of S3 buckets and then find out which one is public .

## Usage

```bash
./script.sh 
```

